# 去阻塞优化（按风险优先级清单）

> 目标：先把会导致“卡死/长阻塞/命令丢失”的问题按风险排序，后续你可以按这个顺序逐步改代码。
> 
> 文档结构严格对应：
> - 我们每一步在解决什么问题
> - 怎么定位问题（看哪些日志）
> - 改了哪些代码（文件/函数）
> - 为什么这样改
> - 最终效果如何

---

## 0. 当前阻塞风险总览（先定优先级）

### P0（必须先做，直接影响“不卡死”）
1. MQTT 发送链路阻塞（ACK/status 发送等待 `>` 和 `OK`）
2. UART2 环形缓冲在阻塞期溢出，导致下行命令丢失
3. WiFi/MQTT 初始化失败后 `while(1)` 永久锁死

### P1（第二阶段，影响“及时响应”）
4. 继电器 100ms 脉冲为阻塞延时，拖慢主循环
5. SD 写入仍是同步阻塞（LEARN_COMMIT 虽已后台化，但写卡仍会拖时序）

### P2（第三阶段，影响“一致性与稳定体验”）
6. 总功率统计未按 `on` 状态过滤，网页展示冲突
7. 日志打印过于密集，串口带宽差时会放大阻塞

---

## 1. 第一步（P0-1）：把 MQTT 发送从阻塞改为非阻塞状态机

### 我们在解决什么问题
- 现在 `Send_Data_Raw()` 是同步等待流程，主循环会被单次发送卡住几百毫秒到数秒。
- 卡住期间不能及时处理新命令、不能及时 `get` 环形缓冲、BL0942 轮询也会变慢。

### 怎么定位问题（看哪些日志）
- 已有日志：
  - `[TX] RAW prompt timeout ...`
  - `[TX] RAW publish timeout ...`
  - `[PERF] cmd=... ack=... ms`
- 需要补的日志（建议）：
  - `[TXSM] state=WAIT_PROMPT elapsed=...`
  - `[TXSM] state=WAIT_OK elapsed=...`
  - `[MAIN] loop_max_cost=... ms`

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `Send_Data_Raw()`（重构为状态机推进，不再整段阻塞）
  - 新增 `ESP8266_TxTask()`（每轮主循环推进一步）
- `src/bsp_esp8266/bsp_wifi_esp8266.h`
  - 新增发送任务接口
- `src/hal_entry.c`
  - 主循环加入 `ESP8266_TxTask()` 调度
- `src/cJSON_handle/cJSON_handle.c`
  - `send_ack()/upload_strip_status()` 改为“入队发送请求”，不直接阻塞等结果

### 关键修改代码（示例）

```c
/* 发送任务状态机：每轮主循环只推进一步 */
typedef enum {
    TX_IDLE = 0,
    TX_SEND_CMD,
    TX_WAIT_PROMPT,
    TX_SEND_PAYLOAD,
    TX_WAIT_OK,
} tx_state_t;

void ESP8266_TxTask(void)
{
    switch (g_tx.state)
    {
        case TX_IDLE: break;
        case TX_SEND_CMD:     /* 发 AT+MQTTPUBRAW */ break;
        case TX_WAIT_PROMPT:  /* 等 '>'，超时可恢复 */ break;
        case TX_SEND_PAYLOAD: /* 发 payload */ break;
        case TX_WAIT_OK:      /* 等 OK，失败重试 */ break;
        default: g_tx.state = TX_IDLE; break;
    }
}
```

### 已落地代码（当前工程真实实现）

文件：`src/bsp_esp8266/bsp_wifi_esp8266.c`

1) 发送参数、队列与状态机定义（已落地）

```c
#define TX_TOPIC_MAX_LEN        80U
#define TX_PAYLOAD_MAX_LEN      640U
#define TX_QUEUE_DEPTH          4U
#define TX_PROMPT_TIMEOUT_MS    1000U
#define TX_SEND_TIMEOUT_MS      100U
#define TX_OK_TIMEOUT_MS        1000U

typedef enum
{
    TX_STATE_IDLE = 0,
    TX_STATE_WAIT_PROMPT,
    TX_STATE_WAIT_PAYLOAD_SENT,
    TX_STATE_WAIT_OK,
} tx_state_t;
```

2) `Send_Data_Raw()` 改为“仅入队立即返回”（已落地）

```c
bool Send_Data_Raw( char * topics , char * data )
{
    if ((topics == NULL) || (data == NULL))
    {
        return false;
    }

    uint16_t data_len = (uint16_t) strlen(data);
    if (data_len == 0U)
    {
        return false;
    }

    if (!tx_queue_push(topics, data, data_len))
    {
        printf("[TX] queue full/drop topic=%s len=%u\r\n", topics, (unsigned) data_len);
        return false;
    }

    return true;
}
```

3) `ESP8266_TxTask()` 分阶段推进（已落地）

```c
void ESP8266_TxTask(void)
{
    uint32_t now = HAL_GetTick();

    if ((g_tx_state == TX_STATE_IDLE) && !g_tx_has_active)
    {
        if (!tx_queue_pop(&g_tx_active))
        {
            return;
        }
        g_tx_has_active = true;
        snprintf(g_tx_cmd_buf, sizeof(g_tx_cmd_buf),
                 "AT+MQTTPUBRAW=0,\"%s\",%u,1,0\r\n",
                 g_tx_active.topic, (unsigned) g_tx_active.payload_len);
        Clear_Buff();
        ESP8266_AT_Send(g_tx_cmd_buf);
        g_tx_state = TX_STATE_WAIT_PROMPT;
        g_tx_deadline = now + TX_PROMPT_TIMEOUT_MS;
        return;
    }

    switch (g_tx_state)
    {
        case TX_STATE_WAIT_PROMPT:      /* 等 '>' */ break;
        case TX_STATE_WAIT_PAYLOAD_SENT:/* 等 TX_COMPLETE */ break;
        case TX_STATE_WAIT_OK:          /* 等 OK */ break;
        default: break;
    }
}
```

文件：`src/hal_entry.c`

4) 主循环接入发送任务（已落地）

```c
while (1)
{
    ESP8266_TxTask();
    handle_uart_json_stream();
    ...
}
```

### 为什么这样改
- 发送流程本质是异步串口交互，适合状态机；不适合同步 `while + delay`。
- 主循环只做“推进一步”，系统会持续响应其他任务。

### 最终效果如何
- 主循环不再被单次发送长时间占用。
- ACK 抖动显著减小；命令链路更稳定。

---

## 2. 第二步（P0-2）：修复 UART2 环形缓冲溢出导致的命令丢失

### 我们在解决什么问题
- 中断里 `g_rx_buf.put()` 失败（缓冲满）未处理，命令会静默丢失。
- 一旦上层阻塞，溢出概率急剧上升。

### 怎么定位问题（看哪些日志）
- 新增并观察：
  - `[RB] overflow uart2 count=...`
  - `[RB] high_watermark=...`
  - `[CMD] json_drop reason=overflow_or_timeout`
- 结合已有：
  - 命令下发但 MCU 无 ACK、无状态变化

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `esp8266_uart2_callback()`：检查 `put` 返回值并累加溢出计数
- `src/circle_buf/circle_buf.c`
  - 增加可选“占用率统计/高水位统计”
  - 视内存情况把 `rx_buf` 从 1024 提升到 2048/4096
- `src/cJSON_handle/cJSON_handle.c`
  - `handle_uart_json_stream()` 增加异常帧重同步和丢帧日志

### 关键修改代码（示例）

```c
/* UART RX 中断里必须检查 put 返回值，避免静默丢包 */
if (g_rx_buf.put != NULL)
{
    if (g_rx_buf.put(&g_rx_buf, data) != 0)
    {
        g_uart2_rb_overflow_cnt++;
    }
}
```

### 为什么这样改
- 命令链路可靠性优先级最高；丢命令比慢更危险。
- 没有溢出计数就无法判断链路是否健康。

### 最终效果如何
- 从“静默丢命令”变成“可观测、可统计、可调参”。
- 在网络抖动或短时阻塞下，命令成功率明显提升。

---

## 3. 第三步（P0-3）：去掉初始化失败后的永久死循环

### 我们在解决什么问题
- `ESP8266_STA_JoinAP()`、`Connect_MQTT()` 等失败时 `while(1)`，设备永久卡死。

### 怎么定位问题（看哪些日志）
- 当前现象：设备卡在某个错误提示后不再输出主循环日志。
- 需要补的日志：
  - `[NET] init_fail step=... retry=... backoff=...`
  - `[NET] enter_degraded_mode`

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `ESP8266_STA_JoinAP()`
  - `Connect_MQTT()`
  - `Subscribes_Topics()`
- 处理策略：
  - `while(1)` 改成“失败返回错误码 + 重试状态机 + 退避”

### 关键修改代码（示例）

```c
/* 错误改为可恢复返回，不再 while(1) 锁死 */
int Connect_MQTT(...)
{
    if (timeout_or_error)
    {
        return -1;
    }
    return 0;
}
```

### 为什么这样改
- 比赛现场/宿舍网络不稳定是常态，系统必须“可恢复”，不能“一错即死”。

### 最终效果如何
- 断网/连网失败时仍能继续本地控制和采样。
- 网络恢复后自动重连，不需要手动复位。

---

## 4. 第四步（P1-1）：继电器 100ms 脉冲改为“非阻塞脉冲任务”

### 我们在解决什么问题
- 当前 100ms 脉冲通过 `R_BSP_SoftwareDelay()` 实现，会阻塞主循环。

### 怎么定位问题（看哪些日志）
- `[PERF] cmd=ON/OFF ack=...ms` 出现固定台阶延迟。
- 新增：
  - `[RELAY] pulse_start socket=...`
  - `[RELAY] pulse_end socket=... cost=...ms`

### 改了哪些代码（文件/函数）
- `src/Relay/Relay.c`
  - `Relay_Set_ON()/Relay_Set_OFF()` 改为“置高并登记截止时刻”
  - 新增 `Relay_Task()` 到时拉低
- `src/Relay/Relay.h`
  - 暴露 `Relay_Task()`
- `src/hal_entry.c`
  - 主循环增加 `Relay_Task()`

### 关键修改代码（示例）

```c
/* 非阻塞脉冲：置高后登记截止时间，到时拉低 */
void Relay_Set_ON(uint8_t index)
{
    relay_ctx[index].active = true;
    relay_ctx[index].deadline = HAL_GetTick() + 100U;
    pinWrite(set_pin[index], HIGH);
}

void Relay_Task(void)
{
    if (relay_ctx[i].active && HAL_GetTick() >= relay_ctx[i].deadline)
    {
        pinWrite(set_pin[i], LOW);
        relay_ctx[i].active = false;
    }
}
```

### 为什么这样改
- 安全需求是“脉冲宽度 100ms”，不是“CPU 必须阻塞 100ms”。

### 最终效果如何
- 保持继电器安全驱动条件不变。
- 命令处理与采样不会被继电器动作打断。

---

## 5. 第五步（P1-2）：SD 写入任务再细化，避免长时间独占

### 我们在解决什么问题
- `AI_Commit_Task()` 已后台化，但真正 `f_open/f_write/f_close` 仍可能长阻塞。

### 怎么定位问题（看哪些日志）
- 已有：`[PERF] learn commit socket=... cost=... ms`
- 需要补：
  - `[SD] open_cost=... write_cost=... close_cost=...`
  - `[MAIN] loop_max_cost=... ms`

### 改了哪些代码（文件/函数）
- `src/Appliance identification algorithm/appliance_identification.c`
  - `AI_Commit_Task()` 增加“每轮只处理一个任务”与超时保护
- `src/sdcard_data_handle/sdcard_data_handle.c`
  - 可选：拆分成小步任务（打开/写入/关闭）

### 关键修改代码（示例）

```c
/* 每轮只处理一个 commit，削峰主循环耗时 */
void AI_Commit_Task(void)
{
    for (uint8_t i = 0; i < AI_SOCKET_NUM; i++)
    {
        if (!g_commit_task[i].valid) continue;
        (void)AI_Commit_Pending(i, ...);
        g_commit_task[i].valid = false;
        break;
    }
}
```

### 为什么这样改
- 减少主循环单次抖动峰值，比追求单次提交最快更重要。

### 最终效果如何
- 命令响应在 SD 慢卡情况下仍可接受。
- 写卡耗时可观测、可定位。

---

## 6. 第六步（P2-1）：修正总功率统计口径，避免网页冲突

### 我们在解决什么问题
- 单路 `power_w` 已按 `on` 过滤，但 `total_power` 仍累加所有路，可能出现展示矛盾。

### 怎么定位问题（看哪些日志）
- 对比 status 包：某路 `on=false`、`power_w=0`，但 `total_power_w` 偏大。

### 改了哪些代码（文件/函数）
- `src/uart_hlw/uart_hlw.c`
  - `Data_Processing()` 中总功率/总电流累加逻辑按 `g_strip.sockets[j].on` 过滤

### 关键修改代码（示例）

```c
float temp_p_sum = 0.0f;
float temp_c_sum = 0.0f;
for (int j = 0; j < 4; j++)
{
    if (!g_strip.sockets[j].on) continue;
    temp_p_sum += g_strip.sockets[j].power;
    if (g_strip.voltage > 1.0f)
    {
        temp_c_sum += (g_strip.sockets[j].power / g_strip.voltage);
    }
}
```

### 为什么这样改
- 统计口径必须和前端展示口径一致，避免用户误判“关了还在耗电”。

### 最终效果如何
- 网页数据显示一致，减少误报警与联调干扰。

---

## 7. 第七步（P2-2）：日志节流，降低串口输出对实时性的反噬

### 我们在解决什么问题
- 高频 `printf` 在串口拥塞时会拖慢系统，放大其他阻塞问题。

### 怎么定位问题（看哪些日志）
- 观察日志密度和命令响应是否同频恶化。
- 新增：
  - `[LOG] dropped_debug_count=...`

### 改了哪些代码（文件/函数）
- `src/event_detector/event_detector.c`
- `src/uart_hlw/uart_hlw.c`
- `src/cJSON_handle/cJSON_handle.c`
- 统一引入日志级别与节流宏（DEBUG/INFO/WARN/ERROR）

### 关键修改代码（示例）

```c
#define LOG_THROTTLE_MS 3000U
if (HAL_GetTick() - last_log_tick >= LOG_THROTTLE_MS)
{
    last_log_tick = HAL_GetTick();
    printf("[LP] socket=%d power=%.2fW\r\n", index, power);
}
```

### 为什么这样改
- 保障关键日志可见，同时不给主循环制造额外负担。

### 最终效果如何
- 调试信息仍够用，但系统时序更稳。

---

## 8. 建议执行顺序（你后续按这个顺序改）

1. P0-1 MQTT 发送状态机化
2. P0-2 UART 缓冲溢出可观测+扩容+重同步
3. P0-3 去掉 `while(1)` 永久锁死，改重试退避
4. P1-1 继电器非阻塞脉冲
5. P1-2 SD 写入细粒度后台化
6. P2-1 修正总功率统计口径
7. P2-2 日志分级与节流

---

## 9. 每一步验收口径（建议固定）

- 命令 ACK 成功率：目标 >= 99%
- 命令到 ACK 耗时：常态 < 300ms，峰值可解释
- 状态上报间隔：心跳稳定，事件上报不抖动
- 环形缓冲溢出计数：持续为 0 或可控
- 网络异常恢复：无需重启可自动恢复
- 网页展示一致性：单路与总量不冲突

---

## 10. 备注

这份文档是“去阻塞改造路线图”。
你后面每做完一步，我可以继续按同样结构帮你写“实施记录版”（含真实改动 diff、真实日志样本、前后对比数据）。

---

## 11. 本次已完成（P2-1）：修正总功率统计口径，避免网页冲突

### 我们这一步在解决什么问题
- 之前存在口径不一致：
  - 单路上报 `power_w` 在 `on=false` 时会归零；
  - 但 `total_power_w/total_current` 仍累计所有路瞬时功率。
- 导致网页可能出现“单路都关了但总功率不为 0”这类冲突显示。

### 怎么定位问题（看哪些日志）
- 重点看 status 包字段一致性：
  - `sockets[i].on=false` 且 `sockets[i].power_w=0`
  - 同时 `total_power_w` 仍异常偏大
- 可补充串口打印（可选）：
  - `[PWRCHK] on_count=... total_power=...`

### 改了哪些代码（文件/函数）
- 文件：`src/uart_hlw/uart_hlw.c`
- 函数：`Data_Processing()`
- 改动点：
  - 总功率/总电流累加时仅统计 `g_strip.sockets[j].on == true` 的通道
  - 总电流计算增加 `voltage > 1.0f` 保护，避免异常电压导致除零风险

### 关键修改代码（本次真实改动）

```c
/* Before */
for (int j = 0; j < 4; j++)
{
    temp_p_sum += g_strip.sockets[j].power;
    temp_c_sum += (g_strip.sockets[j].power / g_strip.voltage);
}
```

```c
/* After */
for (int j = 0; j < 4; j++)
{
    if (!g_strip.sockets[j].on)
    {
        continue;
    }

    temp_p_sum += g_strip.sockets[j].power;
    if (g_strip.voltage > 1.0f)
    {
        temp_c_sum += (g_strip.sockets[j].power / g_strip.voltage);
    }
}
```

### 为什么这样改
- 让总量口径与单路口径一致，前端展示才不会“自相矛盾”。
- 这是低风险改动，不影响识别状态机和命令链路，只修正统计层。

### 最终效果如何
- 网页上单路开关状态与总功率显示一致性提升。
- 可直接减少“误判漏电/关断无效”的联调噪声。

---

## 12. 本次已完成（P0-1）：MQTT 发送从阻塞改为非阻塞状态机

### 我们这一步在解决什么问题
- 原先 `Send_Data_Raw()` 会同步等待 `>`、等待发送完成、等待 `OK`，主循环在这段期间不能处理其他任务。
- 直接后果是：命令解析、功率轮询、按键响应都会被 TX 阻塞拖慢。

### 怎么定位问题（看哪些日志）
- 原有日志特征：
  - `[TX] RAW prompt timeout ...`
  - `[TX] RAW publish timeout ...`
  - `ack` 延迟波动大
- 本次改造后新增可观察点：
  - `[TX] queue full/drop ...`（发送队列压力）
  - 原有 timeout 日志仍保留（但由状态机超时触发）

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `Send_Data_Raw()`：改为“仅入队，不阻塞等待”
  - 新增 `ESP8266_TxTask()`：主循环推进发送状态
  - 新增发送队列与状态机上下文（`TX_QUEUE_DEPTH`、`TX_STATE_*`）
- `src/bsp_esp8266/bsp_wifi_esp8266.h`
  - 新增 `ESP8266_TxTask()` 声明
- `src/hal_entry.c`
  - 主循环接入 `ESP8266_TxTask()`
- `src/cJSON_handle/cJSON_handle.c`
  - ACK/status 发送语义改为“enqueue 成功/失败”
  - 去掉 ACK 里的 `20ms` 阻塞重试

### 关键修改代码（本次真实改动）

```c
/* Before: Send_Data_Raw() 内部阻塞等待 */
sprintf(cmd_buf, "AT+MQTTPUBRAW=0,\"%s\",%d,1,0\r\n", topics, data_len);
ESP8266_AT_Send(cmd_buf);
while (timeout--) { if (strchr(At_Rx_Buff, '>')) break; R_BSP_SoftwareDelay(5, ...); }
...
while (timeout--) { if (strstr(At_Rx_Buff, "OK")) { ok = true; break; } R_BSP_SoftwareDelay(5, ...); }
```

```c
/* After: Send_Data_Raw() 仅入队，真正发送由 TxTask 推进 */
bool Send_Data_Raw(char *topics, char *data)
{
    ...
    if (!tx_queue_push(topics, data, data_len))
    {
        printf("[TX] queue full/drop topic=%s len=%u\\r\\n", topics, (unsigned)data_len);
        return false;
    }
    return true;
}
```

```c
/* 主循环每轮推进一步发送状态机 */
void ESP8266_TxTask(void)
{
    switch (g_tx_state)
    {
        case TX_STATE_WAIT_PROMPT:      /* 等 '>' */ break;
        case TX_STATE_WAIT_PAYLOAD_SENT:/* 等 TX 完成 */ break;
        case TX_STATE_WAIT_OK:          /* 等 OK */ break;
        default: break;
    }
}
```

```c
/* hal_entry 主循环接入 */
while (1)
{
    ESP8266_TxTask();
    handle_uart_json_stream();
    ...
}
```

### 为什么这样改
- 发送过程本质是异步串口交互，状态机能天然避免长阻塞。
- “入队 + 周期推进”可以把单次几百毫秒的阻塞摊平到多轮循环。

### 最终效果如何
- 主循环不再被 `Send_Data_Raw()` 长时间卡住。
- ACK/status 发送与主任务并行推进，系统整体响应更稳定。
- 如果队列满会显式告警（`queue full/drop`），问题可观测、可继续优化。

---

## 13. 本次已完成（P1-1）：继电器 100ms 脉冲改为非阻塞脉冲任务

### 我们这一步在解决什么问题
- 原实现在 `Relay_Set_ON()/Relay_Set_OFF()` 内部直接 `R_BSP_SoftwareDelay(100ms)`。
- 每次继电器动作都会阻塞主循环，导致按键响应、串口收包、MQTT 状态机推进被拖慢。

### 怎么定位问题（看哪些日志）
- 现象日志：
  - `[KEY] keyX pressed ...` 之后 ACK 或状态上报有固定额外延迟。
  - 连续多路开关时主循环吞吐下降明显。
- 建议补充日志（可选）：
  - `[RELAY] pulse_start socket=... type=SET/RESET`
  - `[RELAY] pulse_end socket=... cost=...ms`

### 改了哪些代码（文件/函数）
- `src/Relay/Relay.c`
  - 删除 `Relay_Set_ON()/Relay_Set_OFF()` 中的 `R_BSP_SoftwareDelay(100ms)`
  - 新增 `relay_start_pulse()` 内部助手：置高并登记 `deadline`
  - 新增 `Relay_Task()`：到时拉低并清理上下文
  - `Relay_Reset_All()` 保持接口不变，但底层已改为非阻塞脉冲
- `src/Relay/Relay.h`
  - 新增 `Relay_Task()` 声明
- `src/hal_entry.c`
  - 主循环增加 `Relay_Task()` 调度

### 关键修改代码（本次真实改动）

```c
/* Before: 每次动作阻塞 100ms */
void Relay_Set_OFF(uint8_t index)
{
    g_ioport.p_api->pinWrite(g_ioport.p_ctrl, g_relays[index].reset_pin, BSP_IO_LEVEL_HIGH);
    R_BSP_SoftwareDelay(100, BSP_DELAY_UNITS_MILLISECONDS);
    g_ioport.p_api->pinWrite(g_ioport.p_ctrl, g_relays[index].reset_pin, BSP_IO_LEVEL_LOW);
}
```

```c
/* After: 启动非阻塞脉冲，由 Relay_Task 负责收尾 */
static void relay_start_pulse(uint8_t index, bsp_io_port_pin_t pin)
{
    g_ioport.p_api->pinWrite(g_ioport.p_ctrl, pin, BSP_IO_LEVEL_HIGH);
    g_relay_pulse[index].active = true;
    g_relay_pulse[index].active_pin = pin;
    g_relay_pulse[index].deadline = HAL_GetTick() + RELAY_PULSE_MS;
}

void Relay_Task(void)
{
    uint32_t now = HAL_GetTick();
    for (uint8_t i = 0; i < RELAY_COUNT; i++)
    {
        if (g_relay_pulse[i].active && (int32_t)(now - g_relay_pulse[i].deadline) >= 0)
        {
            g_ioport.p_api->pinWrite(g_ioport.p_ctrl, g_relay_pulse[i].active_pin, BSP_IO_LEVEL_LOW);
            g_relay_pulse[i].active = false;
        }
    }
}
```

```c
/* 主循环接入 */
while (1)
{
    Relay_Task();
    ESP8266_TxTask();
    ...
}
```

### 为什么这样改
- 继电器的硬件需求是“引脚高电平保持约 100ms”，不是“CPU 阻塞 100ms”。
- 把“等待时间”从函数内部移到主循环任务推进，可以保留同样脉冲宽度且不打断其他任务。

### 最终效果如何
- 继电器动作期间主循环可继续处理 UART/MQTT/按键/采样任务。
- 连续多次开关不再叠加 `100ms` 阻塞，系统响应更平滑。

---

## 14. 本次已完成（P0-2 第一步）：UART2 环形缓冲溢出“可观测化”

### 我们这一步在解决什么问题
- 之前 UART2 中断里写环形缓冲时未检查返回值，缓冲满会静默丢字节。
- 静默丢包会导致网页下发 JSON 命令被截断，表现为“偶发无响应/ACK异常”。

### 怎么定位问题（看哪些日志）
- 新增日志：
  - `[RB] overflow uart2 count=... (+...)`
- 含义：
  - `count` 为累计溢出次数；
  - `+...` 为最近 1 秒新增溢出数。

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `esp8266_uart2_callback()`：检查 `g_rx_buf.put()` 返回值，失败时累加溢出计数。
  - `ESP8266_TxTask()`：每 1s 节流打印一次溢出统计（仅计数变化时打印）。

### 关键修改代码（本次真实改动）

```c
/* UART RX 中断：写满时计数，避免静默丢包 */
if (g_rx_buf.put != NULL)
{
    if (g_rx_buf.put(&g_rx_buf, data) != 0)
    {
        g_uart2_rb_overflow_cnt++;
    }
}
```

```c
/* 主循环任务中节流打印：每 1s 最多一次 */
if ((uint32_t)(now - g_uart2_rb_overflow_last_tick) >= 1000U)
{
    if (g_uart2_rb_overflow_cnt != g_uart2_rb_overflow_last_print)
    {
        printf("[RB] overflow uart2 count=%lu (+%lu)\\r\\n", ...);
        g_uart2_rb_overflow_last_print = g_uart2_rb_overflow_cnt;
    }
    g_uart2_rb_overflow_last_tick = now;
}
```

### 为什么这样改
- 先把“看不见的问题”变成“可计数、可定位的问题”，后续扩容和重同步才有依据。
- 节流打印避免日志本身引入额外实时性负担。

### 最终效果如何
- UART2 缓冲溢出不再静默，可直接在串口日志观察风险强度。
- 为下一步（缓冲扩容、超长帧丢弃重同步）提供量化输入。

---

## 15. 本次已完成（P0-2 第二步）：环形缓冲扩容 + JSON 超长帧丢弃重同步

### 我们这一步在解决什么问题
- 命令突发时，`rx_buf` 容量不足导致更容易满缓冲。
- 单条异常长帧会把 JSON 拼包缓冲顶满，进而污染后续解析，造成“连锁解析异常”。

### 怎么定位问题（看哪些日志）
- 环形缓冲溢出：
  - `[RB] overflow uart2 count=... (+...)`
- JSON 超长帧丢弃：
  - `[CMD] json_drop reason=too_long count=... (+...)`

### 改了哪些代码（文件/函数）
- `src/circle_buf/circle_buf.c`
  - `rx_buf` 从 `1024` 扩容到 `4096`（宏 `UART2_RX_BUF_SIZE`）
- `src/cJSON_handle/cJSON_handle.c`
  - `handle_uart_json_stream()` 新增 `drop_current_frame` 机制：
    - 超长后丢弃当前帧，直到 `{}` 计数重新闭合
    - 闭合后恢复正常解析
  - 新增超长丢弃计数与节流日志

### 关键修改代码（本次真实改动）

```c
/* UART2 命令环形缓冲区扩容 */
#define UART2_RX_BUF_SIZE   4096U
uint8_t rx_buf[UART2_RX_BUF_SIZE];
```

```c
/* 超长帧：进入丢弃模式，等待当前帧闭合后重同步 */
if (pos < (MAX_JSON_SIZE - 1))
{
    json_process_buf[pos++] = (char)temp_byte;
    json_process_buf[pos] = '\0';
}
else
{
    json_too_long_drop_cnt++;
    drop_current_frame = true;
    pos = 0;
    json_process_buf[0] = '\0';
}
```

### 为什么这样改
- 扩容可以显著降低“短时拥塞即溢出”的概率。
- 丢弃+重同步可以把异常限制在“当前坏帧”，避免拖垮后续正常命令。

### 最终效果如何
- 命令高峰下稳定性提升，网页下发指令连续性更好。
- 解析链路对异常长帧更鲁棒，不再出现长时间“卡在脏包状态”。

---

## 16. 本次已完成（P1）：分离 AT 响应与云命令接收通道（双缓冲）

### 我们这一步在解决什么问题
- 之前 AT 响应和云命令共用同一接收环形缓冲，存在互相抢占与干扰。
- 典型问题是：AT 状态机等待 `OK/>` 与 JSON 解析同时消费同一数据源，容易出现误判超时或命令解析抖动。

### 怎么定位问题（看哪些日志）
- 命令链路看：
  - `[PERF] cmd=... ack=...`
  - `[CMD] json_drop ...`
- 缓冲链路看：
  - `[RB] overflow cmd_rx count=... (+...)`
- AT 链路看：
  - `[TX] RAW prompt timeout ...`
  - `[TX] RAW publish timeout ...`

### 改了哪些代码（文件/函数）
- `src/circle_buf/circle_buf.c`
  - 新增 `g_cmd_rx_buf`（命令专用环形缓冲）
  - `circlebuf_init()` 同时初始化 `g_rx_buf` 与 `g_cmd_rx_buf`
- `src/circle_buf/circle_buf.h`
  - 新增 `extern circle_buf_t g_cmd_rx_buf;`
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `esp8266_uart2_callback()`：
    - UART2 收到的数据先组装成行
    - 仅当行内包含 `+MQTTSUBRECV` 时，才写入 `g_cmd_rx_buf`
  - 初始化阶段排空对象改为 `g_cmd_rx_buf`
- `src/cJSON_handle/cJSON_handle.c`
  - `handle_uart_json_stream()` 改为仅从 `g_cmd_rx_buf` 取字节解析

### 关键修改代码（本次真实改动）

```c
/* 新增命令专用环形缓冲 */
circle_buf_t g_cmd_rx_buf;
```

```c
/* UART2 回调：仅把 +MQTTSUBRECV 行转发到 cmd_rx */
if (data == '\n')
{
    if ((!g_cmd_line_overflow) && (strstr(g_cmd_line_buf, "+MQTTSUBRECV") != NULL))
    {
        for (uint16_t i = 0; i < g_cmd_line_len; i++)
        {
            if (g_cmd_rx_buf.put(&g_cmd_rx_buf, (uint8_t)g_cmd_line_buf[i]) != 0)
            {
                g_uart2_rb_overflow_cnt++;
                break;
            }
        }
    }
    g_cmd_line_len = 0;
    g_cmd_line_overflow = false;
}
```

```c
/* JSON 解析只消费命令专用缓冲 */
while (g_cmd_rx_buf.get(&g_cmd_rx_buf, &temp_byte) == 0)
{
    ...
}
```

### 为什么这样改
- AT 状态机与云命令解析职责不同，分离缓冲区能从结构上降低耦合和误判。
- 这种改法比“在单缓冲上加更多过滤条件”更稳，也更容易定位问题。

### 最终效果如何
- AT 指令响应与云命令解析互不抢占，误判超时风险下降。
- 网页下发命令在高频场景下稳定性更高，排查路径更清晰（看各自日志即可）。

---

## 17. 本次已完成（P0-3）：初始化失败不再卡死，改为降级运行 + 自动重连

### 我们这一步在解决什么问题
- 之前 WiFi/MQTT 初始化失败会进入永久 `while(1)`，整机主循环停摆。
- 现在改为“失败返回”，主循环继续执行本地功能，并周期重连。

### 怎么定位问题（看哪些日志）
- 初次失败：
  - `[NET] init failed, enter degraded mode`
- 重连过程：
  - `[NET] reconnect attempt idx=...`
  - `[NET] reconnect success`
  - `[NET] reconnect failed, next in ... ms`

### 改了哪些代码（文件/函数）
- `src/bsp_esp8266/bsp_wifi_esp8266.h`
  - `ESP8266_MQTT_Test/ESP8266_STA_JoinAP/Connect_MQTT/Subscribes_Topics` 改为 `bool` 返回
- `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - 三个关键网络步骤移除永久死循环，改为超时/错误返回 `false`
  - `ESP8266_MQTT_Test()` 按步骤判定，任一步失败立即返回 `false`
  - `ESP8266_UART2_Init()` 增加幂等保护，避免重连时重复打开 UART2
- `src/hal_entry.c`
  - 新增 `Net_Task()`，退避重连（1s/2s/5s/10s/30s）
  - 网络未就绪时进入降级模式：本地任务继续，暂停网络上报避免队列堆积

### 关键修改代码（本次真实改动）

```c
/* 失败返回，不再 while(1) 卡死 */
bool ESP8266_STA_JoinAP(char *id, char *password, uint8_t timeout)
{
    ...
    if (esp_wait_response("OK\\r\\n", "ERROR\\r\\n", timeout * 1000U)) return true;
    return false;
}
```

```c
/* 初始化总入口改为 bool 结果 */
bool ESP8266_MQTT_Test(void)
{
    ...
    if (!ESP8266_STA_JoinAP(...)) return false;
    if (!Connect_MQTT(...)) return false;
    if (!Subscribes_Topics(...)) return false;
    return true;
}
```

```c
/* 主循环重连任务 */
static void Net_Task(void)
{
    if (g_net_ready) return;
    if (now < g_net_next_retry_tick) return;
    g_net_ready = ESP8266_MQTT_Test();
    ...
}
```

### 为什么这样改
- 网络抖动是常态，系统应“网络失败可恢复”，而不是“失败即停机”。
- 降级运行能保证按键、继电器、电参采样等核心本地能力不受网络失败影响。

### 最终效果如何
- 初始化失败后，程序不再卡死。
- 本地功能可持续运行，网络恢复后能自动重连。

---

## 18. 本次已完成（P1-2）：SD 写卡改为非阻塞状态机推进

### 我们这一步在解决什么问题
- 原先 `AI_Commit_Task()` 直接调用 `AI_Commit_Pending()`，内部一次性执行 `f_open/f_write/f_close`，会拉长单轮主循环耗时。
- 在慢卡或文件较大时，容易拖慢命令响应和采样节奏。

### 怎么定位问题（看哪些日志）
- 继续看：
  - `[PERF] learn commit socket=... cost=... ms result=...`
- 现在该日志会反映“整体事务耗时”，而不是单轮循环被一次性堵住。

### 改了哪些代码（文件/函数）
- `src/Appliance identification algorithm/appliance_identification.c`
  - 新增 `Commit_Sm_State_t / Commit_Sm_Ctx_t`
  - 新增 `commit_sm_start()`、`commit_sm_step()`、`commit_sm_finalize()`
  - `AI_Commit_Task()` 改为：
    - 空闲时取一个任务启动状态机
    - 每轮仅推进一个状态步骤（open/scan/write/close）

### 关键修改代码（本次真实改动）

```c
/* 每轮只推进一步，避免 SD 操作长时间独占主循环 */
void AI_Commit_Task(void)
{
    if (!g_commit_sm.active)
    {
        ... // 取一个待提交任务，启动状态机
    }
    commit_sm_step();
}
```

```c
/* 状态机核心步骤（节选） */
switch (g_commit_sm.state)
{
    case COMMIT_SM_OPEN:        ... break;
    case COMMIT_SM_SCAN_STEP:   ... break; /* 每次最多扫描一行 */
    case COMMIT_SM_WRITE_HEADER:... break;
    case COMMIT_SM_WRITE_ROW:   ... break;
    case COMMIT_SM_CLOSE:       ... break;
    case COMMIT_SM_DONE:        commit_sm_finalize(); break;
}
```

### 为什么这样改
- SD 写卡本质是慢外设操作，拆成状态机后可以把耗时分散到多轮循环，降低实时任务抖动。
- 保留原有业务语义：成功后更新设备名并清 pending；失败保留 pending，便于重试。

### 最终效果如何
- 主循环峰值阻塞显著下降，命令链路和采样任务更平滑。
- 在慢 SD 卡场景下，系统整体响应稳定性提升。

---

## 19. 本次已完成（P2-2 第一版）：日志分级 + 高频日志节流

### 我们这一步在解决什么问题
- 高频 `printf` 容易占用串口带宽并放大中断负载，进而拖慢主循环。
- 缺少统一日志级别时，发布版也会输出过多调试信息。

### 怎么定位问题（看哪些日志）
- 看是否仍频繁刷屏：
  - `[PERF] status upload interval=...`
  - `[LP] socket=...`
  - `[RB] overflow ...`
- 看关键错误是否仍可见：
  - `[TX] ... timeout`
  - `[NET] reconnect failed ...`

### 改了哪些代码（文件/函数）
- 新增：`src/log.h`
  - 统一 `LOGD/LOGI/LOGW/LOGE`
  - 统一 `LOG_THROTTLE_MS(...)`
  - 默认日志级别 `LOG_LEVEL_INFO`
- 主要替换文件：
  - `src/hal_entry.c`
  - `src/bsp_esp8266/bsp_wifi_esp8266.c`
  - `src/cJSON_handle/cJSON_handle.c`
  - `src/uart_hlw/uart_hlw.c`
  - `src/Appliance identification algorithm/appliance_identification.c`

### 关键修改代码（本次真实改动）

```c
/* 新增统一日志头 */
#define LOG_LEVEL_DEBUG 0
#define LOG_LEVEL_INFO  1
#define LOG_LEVEL_WARN  2
#define LOG_LEVEL_ERROR 3

#define LOG_THROTTLE_MS(last_tick_var, interval_ms, fmt, ...)
```

```c
/* status 高频日志改为 2s 节流 */
static uint32_t s_status_perf_tick = 0;
LOG_THROTTLE_MS(s_status_perf_tick, 2000U,
                "[PERF] status upload interval=%lu ms\r\n",
                (unsigned long)delta_ms);
```

```c
/* 错误/告警改为分级日志 */
LOGW("[TX] RAW publish timeout topic=%s\r\n", g_tx_active.topic);
LOGE("[TX] RAW write fail err=%d topic=%s\r\n", (int)werr, g_tx_active.topic);
```

### 为什么这样改
- 用最小改动先把日志从“裸 printf”统一到“有级别、可节流”的体系，降低实时路径压力。
- 保留关键错误可见性，避免“静默失败”。

### 最终效果如何
- 高频日志刷屏明显下降，串口压力减轻。
- 关键故障日志仍可追踪，联调效率不受影响。
